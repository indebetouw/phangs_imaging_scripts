#########
Key files
#########

The PHANGS-ALMA pipeline is primarily configured through a number of key files. These define things like the directory
structure, the targets to be processed, the location of raw data, and imaging parameters. The outlines for the key
files (and examples) are given below.

.. toctree::
    :maxdepth: 1
    :caption: Types of key files

    keys/master_key
    keys/ms_key
    keys/singledish_key
    keys/cleanmask_key
    keys/distance_key
    keys/window_key
    keys/config_key
    keys/target_key
    keys/moment_key
    keys/derived_key
    keys/imaging_key
    keys/linmos_key
    keys/override_key
    keys/dir_key
